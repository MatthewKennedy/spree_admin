<% content_for :page_title do %>
  <%= link_to Spree.t(:all_countries), spree.admin_countries_url %> / <%= Spree.t(:new_country) %>
<% end %>

<%= form_with model: [:admin, @country], data: {controller: "input--required"} do |f| %>
  <%= render partial: 'form', locals: { f: f } %>

  <%= render partial: 'spree/admin/shared/resource_actions' %>
<% end %>
