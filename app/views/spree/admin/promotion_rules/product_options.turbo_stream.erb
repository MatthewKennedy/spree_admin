<%= turbo_stream.update @stream_id do %>
  <%= collection_select(nil, "promotion[promotion_rules_attributes][#{@promotion_rule.id}][preferred_eligible_values][#{@product.id}]", @product_options, :id, :presentation, { include_hidden: false }, multiple: true, data: { controller: 'ts--select' }) %>
  <%= label_tag 'options', I18n.t('spree.admin.option_values_rule.options') %>
<% end %>
