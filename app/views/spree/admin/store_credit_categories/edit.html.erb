<% content_for :modal_header do %>
  <h5 class="modal-title"><%= @store_credit_category.name %></h5>
  <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
<% end %>

<% content_for :modal_body do %>
  <div class="container">
    <%= form_with model: [:admin, @store_credit_category], method: :patch, data: { turbo_frame: '_top', action: "turbo:submit-end->modal#submitEnd" } do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
    <% end %>
  </div>
<% end %>

<% content_for :modal_footer do %>
  <%= remote_form_submit_button(@object) %>
<% end %>

<%= render partial: 'spree/admin/shared/modal', locals: { modal_dialog_settings: 'modal-dialog-scrollable modal-dialog modal-dialog-centered modal-lg' } %>
